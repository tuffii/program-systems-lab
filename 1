по заданию необходимо модифицировать компилятор так что бы он поддерживал синтаксис и операции из задания моего варианта
мой вариант:

EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED INIT ( 3 );
DCL B DEC FIXED INIT ( 3 );
DCL C BIN FIXED;
C = B * 5;
C = A = C;
END EX11; 

в lab-step-1 реализован компилятор который выполняет сборку и запуск для программы
EXAMP: PROC OPTIONS ( MAIN );
DCL A BIN FIXED ( 31 ) INIT ( 11B );
DCL B BIN FIXED ( 31 ) INIT ( 100B );
DCL C BIN FIXED ( 31 ) INIT ( 101B );
DCL D BIN FIXED ( 31 );
D = A + B − C;
END EXAMP;

в папке tmp есть мои наработки по реализации и отчет о работе
но к этому отчету и реализации у преподователя есть замечания которые тебе необходимо исправить
начни все с чистого листа переиспользуя код из lab-step-1 и подсматривая реализацию в папке tmp но исправляя ее согласно требованиям преподавателя


проанализируй отчет в папке tmp/отчет_3_этап
там лежат методички преподователя и мой отчет


претензии преподавателя (применимы к коду выполненному в папке tmp):
------------------------------------
1) выполнена работа для программы
EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED ( 15 ) INIT ( 11B );
DCL B DEC FIXED ( 3 ) INIT ( 11B );
DCL C BIN FIXED ( 15 );
C = B * 5;
C = A = C;
END EX11;

требуется выполнить для: 
EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED INIT ( 3 );
DCL B DEC FIXED INIT ( 3 );
DCL C BIN FIXED;
C = B * 5;
C = A = C;
END EX11; 

------------------------------------

2) нужно пересмотреть реализацию оператора =
согласно методичке и спецификации языка бинарное = приоритетнее
реплика преподавателя: "= должно работать так что если одинаковые типы то он как сравнение, если разные то еще и конвертировать". возможно бред. подумай что преподаватель имел в виду.

3) DCL A BIN FIXED (15) INIT ( 11B );

зачем тут добавлено это
чем отличается от DCL A BIN FIXED init (3);

переделать синтаксис что бы обрабатывалось правильно как в исходном

4) придумать LIT но для дисятичной на пример LID

5) 15. ⟨ZNK⟩ ::= + | - | * | = 
разделить знаки орифметики и знак = которое bool. разделить

6) 8. ⟨ODC⟩ ::= DCL_⟨IPE⟩_BIN_FIXED(⟨RZR⟩);
| DCL_⟨IPE⟩_BIN_FIXED(⟨RZR⟩)INIT(⟨LIT⟩);
| DCL_⟨IPE⟩_DEC_FIXED(⟨RZR⟩);
| DCL_⟨IPE⟩_DEC_FIXED(⟨RZR⟩)INIT(⟨LIT⟩);
байнери фиксен без разрядности и инициализации для примера
байнари фиксед без разрядности но с инициализацей

с разрядностью и инициализацией?

всего 5 должно быть

7) 
оператор присвоения OPL (логический) в пункт 13
AVI -> LVI

в 15 ZNL - знак логический для =

в 7 в TEL нужно добавить поддержку этого ZNL



с преподавателем обсуждался отчет до Listing 2. Новые записи таблицы SINT[] (правила 201–231)

все правки что я описал выше касаются только того что в отчете описано до этого раздела.

твоя задача:
скопировать реализацию из step1 в step2 и внести правки что бы компилятор поддерживал синтаксическую обработку моего варианта программы
при этом сохранив возможность работы с программой для которой он разрабатывался (пример преподавателя)

также в папке lab-step-2 создай папку для отчета и создай там отчет с помошью latex аналогичный тому что в tmp но обрезанный до 'Listing 2. Новые записи таблицы SINT[] (правила 201–231)' и с приведенными исправлениями согласно тому что я тебе написал и тому что ты реализуешь

в результате ты должен получить полностью рабочий первый этап работы и корректный отчет с правильно продуманными правилами обработки синтаксиса и тд

реализуй все очень внимательно и отвественно

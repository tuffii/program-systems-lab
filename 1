по заданию необходимо модифицировать компилятор так что бы он поддерживал синтаксис и операции из задания моего варианта
мой вариант:

EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED INIT ( 3 );
DCL B DEC FIXED INIT ( 3 );
DCL C BIN FIXED;
C = B * 5;
C = A = C;
END EX11; 

в lab-step-1 реализован компилятор который выполняет сборку и запуск для программы
EXAMP: PROC OPTIONS ( MAIN );
DCL A BIN FIXED ( 31 ) INIT ( 11B );
DCL B BIN FIXED ( 31 ) INIT ( 100B );
DCL C BIN FIXED ( 31 ) INIT ( 101B );
DCL D BIN FIXED ( 31 );
D = A + B − C;
END EXAMP;

в папке tmp есть мои наработки по реализации и отчет о работе
но к этому отчету и реализации у преподователя есть замечания которые тебе необходимо исправить
начни все с чистого листа переиспользуя код из lab-step-1 и подсматривая реализацию в папке tmp но исправляя ее согласно требованиям преподавателя


проанализируй отчет в папке tmp/отчет_3_этап
там лежат методички преподователя и мой отчет


претензии преподавателя (применимы к коду выполненному в папке tmp):
------------------------------------
1) выполнена работа для программы
EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED ( 15 ) INIT ( 11B );
DCL B DEC FIXED ( 3 ) INIT ( 11B );
DCL C BIN FIXED ( 15 );
C = B * 5;
C = A = C;
END EX11;

требуется выполнить для: 
EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED INIT ( 3 );
DCL B DEC FIXED INIT ( 3 );
DCL C BIN FIXED;
C = B * 5;
C = A = C;
END EX11; 

------------------------------------

2) нужно пересмотреть реализацию оператора =
согласно методичке и спецификации языка бинарное = приоритетнее
реплика преподавателя: "= должно работать так что если одинаковые типы то он как сравнение, если разные то еще и конвертировать". возможно бред. подумай что преподаватель имел в виду.

3) DCL A BIN FIXED (15) INIT ( 11B );

зачем тут добавлено это
чем отличается от DCL A BIN FIXED init (3);

переделать синтаксис что бы обрабатывалось правильно как в исходном

4) придумать LIT но для дисятичной на пример LID

5) 15. ⟨ZNK⟩ ::= + | - | * | = 
разделить знаки орифметики и знак = которое bool. разделить

6) 8. ⟨ODC⟩ ::= DCL_⟨IPE⟩_BIN_FIXED(⟨RZR⟩);
| DCL_⟨IPE⟩_BIN_FIXED(⟨RZR⟩)INIT(⟨LIT⟩);
| DCL_⟨IPE⟩_DEC_FIXED(⟨RZR⟩);
| DCL_⟨IPE⟩_DEC_FIXED(⟨RZR⟩)INIT(⟨LIT⟩);
байнери фиксен без разрядности и инициализации для примера
байнари фиксед без разрядности но с инициализацей

с разрядностью и инициализацией?

всего 5 должно быть

7) 
оператор присвоения OPL (логический) в пункт 13
AVI -> LVI

в 15 ZNL - знак логический для =

в 7 в TEL нужно добавить поддержку этого ZNL



с преподавателем обсуждался отчет до Listing 2. Новые записи таблицы SINT[] (правила 201–231)

все правки что я описал выше касаются только того что в отчете описано до этого раздела.

твоя задача:
скопировать реализацию из step1 в step2 и внести правки что бы компилятор поддерживал синтаксическую обработку моего варианта программы
при этом сохранив возможность работы с программой для которой он разрабатывался (пример преподавателя)

также в папке lab-step-2 создай папку для отчета и создай там отчет с помошью latex аналогичный тому что в tmp но обрезанный до 'Listing 2. Новые записи таблицы SINT[] (правила 201–231)' и с приведенными исправлениями согласно тому что я тебе написал и тому что ты реализуешь

в результате ты должен получить полностью рабочий первый этап работы и корректный отчет с правильно продуманными правилами обработки синтаксиса и тд

реализуй все очень внимательно и отвественно





привет, твоя задача на сегодня - внесение правок в отчет для lab-step-2
он находится в папке lab-step-2/report/отчеты
отчет составлен в формате latex.
твоя задача создать в папке report подкаталоги 1 и 2
в 1 сохранить то что щас лежит в папке report/отчеты а в 2 поместить этот же отчет но с твоими исправлениями

требования к исправлениям:
обрезаем отчет до "Listing 2. Новые записи таблицы SINT[] (правила 201–231)"

будем вносить правки в отчет до этой строки.

правки:
1) некорректно указан вариант задания 11
вот корректная формулировка
EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED INIT ( 3 );
DCL B DEC FIXED INIT ( 3 );
DCL C BIN FIXED;
C = A = ( B * 5 );
END EX11; 

сразу после указания этого кода необходимо пояснить что по договоренности с преподавателем были внесены изменения касательно использования скобок в арифметических выражениях
и была сформулирована новая входная программа:
EX11: PROC OPTIONS ( MAIN );
DCL A BIN FIXED INIT ( 3 );
DCL B DEC FIXED INIT ( 3 );
DCL C BIN FIXED;
C = B * 5;
C = A = C;
END EX11; 


*обрати внимание что в отчете программа была указана не корректно. в ней использовались определения переменных с разрядностью. это нужно учитывать в дальнейших правках. программа должна поддерживать ввод при определении переменной в виде DCL A BIN FIXED INIT ( 3 ); DCL B DEC FIXED INIT ( 3 ); DCL C BIN FIXED;*

2) в папке *источники* лежит фотография image.png
ее нужно использовать как Рис. 2. Схема функционирования учебной системы программирования вместо схемы которая используется в отчете щас

3) в пункте Контрольный (демонстрационный) пример 
исправь код варианта 11 на корректный

поправь список необходимых изменений

4) замени Рис. 4. Схема функционирования модифицированного макетного компилято-ра с ЯВУ на рисунок image2.png из папки источников

5) на Рис. 5. Эталон ассемблерного эквивалента персонального варианта № 11 в таблице слиплись заголовки МеткаКОПОперандыПояснения. исправь

6) удали раздел Согласованные функциональные ограничения

7) далее очень важно все корректно отредактировать в блоке
Модификация состава и структуры БД макета компиля-тора с ЯВУ

правки для Модификация БД синтаксического анализатора:
1) подумать корректно ли описан 7 пункт: ⟨TEL⟩ ::= ⟨ODC⟩ | ⟨TEL⟩⟨ODC⟩ | ⟨TEL⟩⟨OPA⟩  будет ли это корректно работать с тройным оператором из моей задачи?
2) для 8 пункта нужно исправить вариации что бы корректно обрабатывалось определние переменных из обеих задач
3) требуется отдельно ввести аналог LIT но для дисятичной записи. на пример LID
4) для 15 пункта подумай как можно грамотно определить отдельное множество для логических знаков (логический знак =). нужно что бы осталось и ZNK и создать новый ZNL для логических операций. определять его от арифметического знака мы будем по месту в котом он используется
5) в остальных определениях добавь подедржку нового ZNL. проверь что все корректно сочетается
6) проверь что в таблице Таблица 1. Нетерминальные символы грамматики не трубуются правки относительно того что мы обсудиоли выше.

в результате ты должен получить корректный отчет с правками.
задумывайся на каждом шаге корреткно ли это будет. возможно ли будет это реализовать зная что представленный вариант уже работает.
сделай корректный качественный отчет согласно предоставленным правкам




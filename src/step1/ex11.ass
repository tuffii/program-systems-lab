EX11     START 0            Program start. The relative address counter is initi         BALR  @RBASE,0     Load the current program address into the base regis         USING *,@RBASE     Declare @RBASE as the base register for addressing p         ZAP   @BUF(8),B(3) Copy the packed decimal value of B into the working          MP    @BUF(8),@F(1 Multiply the decimal value in @BUF by the constant 5         CVB   @R1,@BUF     Convert the packed decimal result in @BUF to binary          STH   @R1,C        Store the binary result from register @R1 into varia         LH    @R2,A        Load the binary value of variable A into register @R         CH    @R2,C        Compare the value in register @R2 with the value sto         BC    8,@TRUE      Branch to label @TRUE if the values are equal.               LA    @R3,0        Load value 0 into register @R3 (boolean FALSE).              BC    15,@SAVE     Unconditional branch to label @SAVE.                @TRUE    LA    @R3,1        Load value 1 into register @R3 (boolean TRUE).      @SAVE    STH   @R3,C        Store the boolean result (0 or 1) into variable C.           BCR   15,@RVIX     Return control to the calling program using the retuA        DC    H'3'         Declare binary variable A (halfword) initialized witB        DC    PL3'3'       Declare decimal variable B in packed decimal format C        DS    H            Reserve storage for binary variable C (halfword).   @F       DC    PL1'5'       Declare packed decimal constant with value 5.                DS    0F           Align the next data item to a fullword boundary.    @BUF     DC    PL8'0'       Working buffer used for packed decimal arithmetic op@RBASE   EQU   5            Assign register number 5 as the base register.      @R1      EQU   6            Register used for decimal-to-binary conversion.     @R2      EQU   2            Register used for comparison operations.            @R3      EQU   3            Register used to store the boolean result.          @RVIX    EQU   14           Register used to return control to the operating sys         END                End of the assembly program.                        